# Frontend Environment Variables

# API URL (defaults to /api which uses the Vite proxy in development)
VITE_API_URL=/api

# Environment
VITE_ENVIRONMENT=development

# ==========================================
# Azure AD Authentication Configuration
# ==========================================
# Set VITE_AUTH_MODE to 'azure' to enable Azure AD authentication
# Leave as 'mock' for local development without Azure AD
VITE_AUTH_MODE=mock

# Azure AD App Registration (required when AUTH_MODE=azure)
# Get these values from Azure Portal > App Registrations > Your App
VITE_AZURE_CLIENT_ID=
VITE_AZURE_TENANT_ID=

# Redirect URI (optional - defaults to window.location.origin)
# Must match the redirect URI configured in Azure AD App Registration
# VITE_AZURE_REDIRECT_URI=http://localhost:3000
